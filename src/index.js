import React from "react";
import ReactDOM from "react-dom/client";//in react version 18, it has changed


import App from "./App";
import './index.css'; 

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<App />);